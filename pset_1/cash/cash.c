#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // getting user input
    int change;
    do
    {
        change = get_int("Change owed: ");
    }
    while (height <= 0);

    // counting the minimum # of coins
    int coins = 0;
    while (change != 0)
    {
        if (coins%25)
    }
}